<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>🍳 Setor da Cozinha | Painel CB Systems</title>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&family=Playfair+Display:wght@600&display=swap" rel="stylesheet">
<style>
:root {
  --bg: #f4f1ec;
  --card: #fffaf5;
  --accent: #b8860b;
  --ink: #3a2e1f;
  --radius: 14px;
  --shadow: 0 4px 14px rgba(0,0,0,0.08);
}
*{box-sizing:border-box;margin:0;padding:0;font-family:'Inter',sans-serif}
body{
  background:var(--bg);
  color:var(--ink);
  min-height:100vh;
  display:flex;
  flex-direction:column;
  align-items:center;
  justify-content:flex-start;
  padding:40px 20px;
}
header{
  text-align:center;
  margin-bottom:40px;
}
header h1{
  font-family:'Playfair Display',serif;
  color:var(--accent);
  font-size:2rem;
  letter-spacing:1px;
}
.grid{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(200px,1fr));
  gap:20px;
  width:100%;
  max-width:900px;
}
.card{
  background:var(--card);
  border-radius:var(--radius);
  box-shadow:var(--shadow);
  padding:25px 20px;
  text-align:center;
  transition:transform .2s, box-shadow .2s;
  cursor:pointer;
}
.card:hover{
  transform:translateY(-5px);
  box-shadow:0 6px 20px rgba(0,0,0,0.1);
}
.card-icon{
  font-size:40px;
  margin-bottom:10px;
}
.card-title{
  font-weight:600;
  color:var(--ink);
  font-size:1.1rem;
}
footer{
  margin-top:auto;
  padding:20px;
  font-size:.85rem;
  color:#777;
  text-align:center;
}
</style>
</head>
<body>

<header>
  <h1>🍳 Painel da Cozinha</h1>
  <p>Acesso rápido às funções do setor</p>
</header>

<main class="grid">
  <div class="card" onclick="abrir('reservas.html')">
    <div class="card-icon">📅</div>
    <div class="card-title">Reservas</div>
  </div>

  <div class="card" onclick="abrir('requisicoes.html')">
    <div class="card-icon">🧾</div>
    <div class="card-title">Requisições</div>
  </div>

  <div class="card" onclick="abrir('cardapio.html')">
    <div class="card-icon">🍲</div>
    <div class="card-title">Cardápio</div>
  </div>

</main>

<footer>© 2025 CB Systems — Setor da Cozinha</footer>

<script>
function abrir(pagina){
  window.location.href = pagina;
}
</script>

</body>
</html>
